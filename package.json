{
  "name": "hermes-chat-app",
  "version": "0.1.0",
  "private": true,
  "description": "Hermes Chat — pure Kotlin WebView APK",
  "scripts": {
    "build": "cd android && ./gradlew assembleRelease"
  }
}
