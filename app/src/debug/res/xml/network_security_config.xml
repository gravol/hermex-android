<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only overrides for network security config.

  Allows cleartext (HTTP) traffic to:
  - localhost (127.0.0.1, ::1)
  - private IP ranges (10.x.x.x, 172.16-31.x.x, 192.168.x.x)

  This lets the app talk to a local Hermes server during development
  without certificate setup.
-->
<network-security-config>
    <!-- Allow cleartext to localhost (IPv4 + IPv6) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- Allow cleartext to private IP ranges -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.0.0/8</domain>
        <domain includeSubdomains="false">172.16.0.0/12</domain>
        <domain includeSubdomains="false">192.168.0.0/16</domain>
        <domain includeSubdomains="false">127.0.0.0/8</domain>
        <domain includeSubdomains="false">::1/128</domain>
    </domain-config>
</network-security-config>
